import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5F6F65', // Primary color
    },
    secondary: {
      main: '#808D7C', // Secondary color
    },
    background: {
      default: '#C9DABF', // Background color
    },
    text: {
      primary: '#9CA986', // Text color
    },
  },
});

export default theme;
